{% comment %} snippet to organise the product {% endcomment %}
<div class="products-container">
  <a href="{{ product.url }}">
    <img src=" {{ product.featured_image | image_url: width:400 }}" alt="">
    <h3>{{ product.title }}
    </h3>
    <p>{{ product.price | money }}
    </p>
  </a>
</div>